apiVersion: v1
kind: Service
metadata:
  name: app-develop-service
  namespace: bladex
  labels:
    app: app-develop-service
spec:
  ports:
  - port: 7007
    targetPort: 7007
    protocol: TCP
  selector:
    app: app-develop-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-develop-service
  namespace: bladex
  labels:
    app: app-develop-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-develop-service
  template:
    metadata:
      labels:
        app: app-develop-service
    spec:
      #hostNetwork: true
      containers:
      - name: app-develop-pod
        image: 192.168.127.158:5000/app-develop-service:1.0.0
        imagePullPolicy: IfNotPresent
        env:
        - name: PARAMES
          value: -Djava.security.egd=file:/dev/./urandom -Dspring.cloud.sentinel.transport.dashboard=192.168.127.137:38858 -Dspring.redis.host=192.168.127.137  -Dspring.redis.port=36379 -Dspring.cloud.nacos.discovery.server-addr=192.168.127.137:38848 -Dspring.cloud.nacos.config.server-addr=192.168.127.137:38848 -Dblade.datasource.dev.username=root -Dblade.datasource.dev.password=root -Dblade.datasource.dev.url=jdbc:mysql://192.168.127.137:33306/bladex?useSSL=false&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&tinyInt1isBit=false&allowMultiQueries=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
        - name: TZ
          value: Asia/Shanghai
        ports:
        - containerPort: 7007
        volumeMounts:
        - mountPath : /app/develop/logs/blade-develop
          name: log-vol
      volumes:
      - name: log-vol
        hostPath:
          path: /home/logs/blade-develop